# embedded-menu-calorie-system
This project implements an embedded system that displays food menus and their calorie values. The user can compare the selected menu with a predefined calorie limit. The system is implemented in C using an AVR microcontroller, buttons, a seven-segment display, and a buzzer for audio feedback.
